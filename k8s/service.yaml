apiVersion: v1
kind: Service
metadata:
  name: mlops-serve-svc
  namespace: mlops
spec:
  selector:
    app: mlops-serve
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer